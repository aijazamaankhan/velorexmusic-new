node_modules/
test-results/
screenshot-*.png

# Real DB credentials live here — never commit
api/secrets.local.php
api/secrets.php
# Legacy path — kept ignored in case anyone still has a local copy
api/config.local.php

# OS / editor
.DS_Store
*.log
.vscode/.ropeproject/

# Playwright
playwright-report/
.mcp.json
.test-images/
.playwright-mcp/
