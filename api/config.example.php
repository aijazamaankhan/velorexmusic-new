<?php
// DEPRECATED — this file is no longer the source-of-truth template.
//
// `api/config.php` is now tracked in git and contains only helpers (no secrets).
// Secrets live in a separate file loaded by config.php — see api/secrets.example.php
// for the new template and the comments at the top of api/config.php for the
// path lookup order.
//
// This stub is kept so existing bookmarks / docs don't 404. Safe to delete.
